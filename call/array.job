#!/bin/bash
#$ -N freq_array
#$ -cwd
#$ -j y
#$ -o logs/
#$ -pe smp 1

export OMP_NUM_THREADS=1

CMD=$(sed -n "${SGE_TASK_ID}p" call.txt)

echo "Task $SGE_TASK_ID running:"
echo "$CMD"

eval "$CMD"

echo "Finished $(date)"
